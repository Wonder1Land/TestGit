spring:
  profiles:
    active: deve
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/create
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true